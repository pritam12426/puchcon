{
    "cSpell.ignorePaths": [
        "**/src/*.txt",  // the same for this one
        "**/src/*.yaml",  // the same for this one
        ".git",                // Ignore the .git directory
    ],
    "files.associations": {
        "*.txt": "c",
        "unistd.h": "c",
        "stdio.h": "c"
    },
}